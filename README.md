# wordpress-beats-playground
A browser-based WordPress demo environment for the Beats Upload Player and Beats Visualizer plugins. This repo contains the Playground configuration that installs both plugins automatically, activates them, and generates a live demo page so users can test all features instantly with no setup required.
